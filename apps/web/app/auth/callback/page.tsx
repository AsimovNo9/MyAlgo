export default function AuthCallbackPage() {
  return (
    <main style={{ padding: 24 }}>
      <h1>Signing in…</h1>
      <p>Your Supabase auth redirect is in place. Once the project is configured, this page will complete the OAuth flow.</p>
    </main>
  );
}
